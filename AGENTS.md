# Linguagram 项目规则

## 工程思维

- 先做最简单、清晰、可验证的实现；不为未来假设预埋代码。
- 修改前先复用项目已有能力；仅当同一规则确实被多个位置使用时才抽成共享模块。
- 不为微小视觉或交互效果新增依赖、状态层、包装组件或复杂动效；每一层新增代码都必须有明确收益。
- 功能增加允许带来必要代码，但应避免重复逻辑、无用配置和一次性抽象；完成后删除死代码并收敛重复状态。
- 优先维护可读性与长期维护成本：业务意图靠近使用点，共享规范集中管理。
- 验证与风险匹配：常规 UI 微调使用构建和静态检查；仅在状态流转、真实数据或用户明确要求时进行浏览器自动化验证。
